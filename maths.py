import math

print(math.pi)  # prints pi


def add(x, y):
    return x + y


# variable = arguments: expression
addition = lambda x, y: x + y  # calculating the expression, can be any expression

print(addition(2, 2))
print(add(2, 2))
